apiVersion: v1
kind: Pod
metadata:
  name: redis-pod
  labels:
    name: redis-pod
    app: demo-voting-app
spec:
  containers:
  - name: redis:latest
    image: dockersamples/examplevotingapp_worker
    ports:
    - containerPort: 6379